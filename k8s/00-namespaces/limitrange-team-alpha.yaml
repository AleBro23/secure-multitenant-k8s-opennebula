apiVersion: v1
kind: LimitRange
metadata:
  name: team-alpha-limits
  namespace: team-alpha
spec:
  limits: # specifies the default and maximum compute resources that can be consumed by containers in the team-alpha namespace
    - type: Container
      default:
        cpu: "250m"
        memory: 256Mi
      defaultRequest:
        cpu: "100m"
        memory: 128Mi
      max:
        cpu: "1"
        memory: 1Gi
      min:
        cpu: "50m"
        memory: 64Mi